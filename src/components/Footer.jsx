const Footer = () => {
    return (
        <footer className="text-center py-6 border-t border-white/10 text-gray-300">
            © {new Date().getFullYear()} Vishal Gunawad. Built with React & Tailwind.
        </footer>
    );
};

export default Footer;
